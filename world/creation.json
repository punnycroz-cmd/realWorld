{
  "doc": "Character creation — machine-readable mirror of world/creation-ui.md (canonical for behavior + copy). 'Joining the cast' per design §6: creating/hiring a character costs credits AND requires renting in-game housing in game dollars. New characters are not exempt from the sim. Prices adopt rw-monetization-plan-2026-09-22 §2.4–2.5 (PROPOSAL — never contradict in player-facing copy).",
  "version": 27,
  "price": {
    "hire_cr": 500,
    "billing": "flat, one-time, charged on approval only (billOnApproval per game-v8) — the charge lands after screening + human review pass",
    "includes": ["character slot", "basic wardrobe", "starter apartment hunt"],
    "refund_rule": "denied applications never bill; a post-review denial auto-refunds in full",
    "slot_cap": { "base": 1, "resident_sub": 2, "director_sub": 3, "hard_max_per_account": 3 }
  },
  "record_schema": {
    "id": "h## — hired-character ids, disjoint from C1–C8 mains and A01–A20 ambients",
    "owner": "player handle — the only account that may ever possess this character",
    "name": "screened string; never a cast/ambient name, never a real person, never slurs",
    "goes_by": "optional block name — what the neighbors end up calling them; screened text, collision-checked against the same registry; blank = the block decides",
    "age": "integer >= 18 — player-possessable characters are adults, always",
    "pronouns": "free pick",
    "bio": "public profile text — screened; this IS the briefing-safe surface",
    "look": {
      "doc": "structured pickers only — rendering data, not text; no free-text look field exists, so nothing here needs screening",
      "build": ["slight", "compact", "tall", "broad", "angular"],
      "palette": ["fog grey", "mission mural", "moss", "midnight", "ochre", "plum"],
      "signature": ["windbreaker, always", "paint-flecked work boots", "loud prints, louder laugh", "cardigans and a paperback", "running gear at dawn", "denim jacket, pins on the collar"]
    },
    "arrival": "one-line 'first day on the block' context — screened text",
    "job_id": "jobs.json row with openings, or 'seeking'",
    "unit_id": "housing.json listings_live unit, or a room tier",
    "bank_game_dollars": "starting balance — rent comes out of this, not credits",
    "brain": "full LLM when possessed or ambient-active; thin AI when owner offline"
  },
  "screening": {
    "surface": ["name", "goes_by", "bio", "arrival"],
    "engine": "world/screen.js RWScreen.screenRequest — the SHARED engine, same code path as request.html + mod-console.html; creation adds only the name-collision registry check (not an intent class). If the engine is unavailable the application cannot be billed — never fall back to private rules.",
    "resubmit_rule": "denied applications may be edited and resubmitted; resubmissions carry resubmit_n and route to a DIFFERENT reviewer (moderation.json appeal-resubmit). No resubmit fee.",
    "rules": "world/moderation-notes.md §3 verbatim — naming strings always get a human review pass (§4)",
    "deny_codes": ["name-collision", "real-person", "harm-targeting", "secret-extraction", "real-business", "legal-backstop", "underage"],
    "note": "Screening covers what the player WROTE. Once created, the character's emergent behavior is inviolable — no moderation, ever."
  },
  "what_the_player_authors": [
    "initial conditions only: identity surface, look, job application, lease",
    "the character's inner life, secrets, and decisions emerge — players never write scripts, triggers, or loyalties",
    "a hired character may decline a nudge like any main; possession is session-scoped"
  ],
  "sim_obligations": [
    "pays rent monthly in game dollars — arrears and eviction apply like anyone else",
    "works their job; thin-AI auto-shifts pay baseline wages",
    "possession during a shift earns the same wage + small session bonus (never pay-to-earn-better)",
    "offline owner → character drops to thin AI (cheap), wakes to full brain on return"
  ],
  "economics_shown_before_signing": {
    "arrival_bank_game_dollars": 1600,
    "rent_to_income_ceiling_pct": 55,
    "ceiling_rule": "units above the ceiling are visible but unselectable — the honest 'out of reach' state at pick time, never a surprise denial after payment",
    "runway_rule": "if projected income < rent, the UI shows months-until-broke on post-move-in cash (+wages); 'arrives without work' is allowed but the runway is displayed verbatim",
    "currency_wall": "rent is game dollars only — credits are never offered for it anywhere in the flow"
  },
  "move_in_math": {
    "doc": "v91 — the production hire package (41_game_systems_hiring.js gsHireMoveIn/gsHireQuote) supersedes the v35 deposit story. Deposit $0 — waived, recorded as hirePackage on the lease ('the hire package covers the hunt'). First month is pro-rated to the move-in day and auto-paid out of the $1,600 arrival bank. Ordinary leases still charge deposits per leases.json — the waiver is the hire package, and a later re-house is a normal second lease with a real deposit.",
    "deposit": "$0 — waived on the hire package only",
    "first_month": "pro-rated: move-in day through month end (the 1st of a month pays the full rent)",
    "rehouse_rule": "a re-house is a normal second lease — deposit owed like anyone else's (rehouse request, 150 cr per production ratePerMin 30 × 5 min)",
    "day_one_cash": "arrival bank − pro-rated first month"
  },
  "payday": {
    "doc": "shifts.md cadence rendered per job row and in the first-week step",
    "weekly": "entry + informal roles — Friday, end of day; the first true paycheck lands the Friday after the 5th",
    "biweekly": "mid/top roles — every other Friday"
  },
  "job_board": {
    "doc": "the whole posted board renders — open and filled. jobs.json is canonical.",
    "openings": "finite counts shown verbatim (n openings); -1 = always hiring (churn gig)",
    "filled": "openings=0 rows stay visible behind a fold, unselectable — the board reads honestly instead of showing only open doors",
    "channels": "word-of-mouth posts (market.json channel) carry the label — never a posted card, ask at the counter",
    "seeking": "'arrives without work' is always selectable — savings only"
  },
  "live_seam": {
    "doc": "create.html reads production bridge surfaces when __aiBridge is present; mirrors otherwise. One write verb — the filing itself.",
    "reads": ["gsJobBoard()", "gsHireNameCheck(name)", "gsHireQuote(spec)", "gsHireSlots(pid)", "gsVacantUnits()", "gsHiredRoster()", "gsExplainRequest(id)"],
    "writes": ["gsSubmitRequest(spec) — the hire filing, real bus vocabulary", "gsCancelRequest(id) — withdraw a pending live filing"],
    "never": ["gsRequestSubmit (legacy alias — the bus's real name is gsSubmitRequest)", "gsHireActivate", "gsHiredTakeJob", "gsApplyForLease", "gsSignLease", "gsReviewResolve (admin-side)", "fetch", "XHR"],
    "badge": "header shows 'live board' | 'demo board' — the page never pretends to be live",
    "honesty": "a live filing never gets a fake approval — 'in_review' parks in the naming lane and the queue card shows the real record id; the demo auto-reviewer runs off-bridge only"
  },
  "hire_seam_v91": {
    "doc": "the production bus shape create.html files when bridged",
    "spec": { "playerId": "you", "kind": "hire", "target": "<unit id>", "durationMin": 5,
      "params": ["name", "goes_by", "age", "pronouns", "bio", "arrival", "look{build,palette,signature}", "job", "moveInDate", "resubmit_n"] },
    "record": "denied → pre-billing refusal, real reason code shown; in_review + lane 'naming' → deferred billing, charge lands at approval; the office stamps one hire per account per 24 h (cdPlayerMin 1440)",
    "deny_codes": ["needs_housing", "unknown_unit", "unit_not_livable", "unit_offmap", "unit_occupied", "hire_cap", "cast_cap", "needs_name", "name_too_short", "name_too_long", "name_collision", "underage", "bad_age", "bad_look", "unknown_job", "job_filled", "out_of_reach", "cooldown", "appeal_final", "insufficient_credits"],
    "note": "goes_by rides params on the record but 41_game_systems_possession's screen haystack doesn't read it yet — flagged for the game track"
  },
  "people_layer": {
    "doc": "v49 — the block's actual names, mirrored from characters.json (cast, k:'cast') and ambients.json (faces, k:'face'). Every row re-verified by the audit gate: w employer tokens resolve into the person's job/venue field, b building prefixes resolve into cast home fields, owns prefixes resolve into the landlord's job field. Ambient homes are not addresses, so faces never carry b.",
    "fields": { "n": "display name (cast full name / ambient full name)", "k": "cast | face", "w": "job-board employer strings — crew card source", "b": "building prefix '9xxx Street' — neighbor card source (cast only)", "owns": "buildings where the registry names this person landlord of record", "note": "public-observable one-liner, briefing-safe" },
    "renders": ["taken-names fold (step 1)", "'you'd work alongside' card (step 3)", "'the building you'd land in' card + landlord-of-record line (step 4)", "FACES row (step 5)", "named surface relationships in the briefing"],
    "honesty": "empty states are stated, not padded — 'no regulars on the card yet — the crew introduces itself on shift'; names shown are surface ties, never claimed friendships"
  },
  "names_registry": {
    "doc": "v49 — the complete taken list rendered on the table (fold on step 1): 8 cast names, 20 ambient first names, 3 role words. Every listed string must match the TAKEN regex — the audit gate checks both directions' agreement.",
    "groups": ["cast", "faces", "roles"]
  },
  "arrival_window": {
    "doc": "v49 — a declared landing window rides the hire, like every request declares its action upfront. Part of the 500 cr, never priced separately.",
    "options": ["lands tonight", "tomorrow morning", "Saturday morning"],
    "default": "tomorrow morning",
    "feed": "the cast event carries it — 'lands <window>' — so the block sees the arrival on The Wire"
  },
  "registry_entry": {
    "doc": "v49 — after the briefing, the minted h## record renders field-for-field per record_schema: id/owner, name/age/pronouns, look, arrival+landing, job_id, unit_id, bank after move-in (game dollars), brain mode. Honesty line: 'the whole file — no secret fields exist on it.'",
    "rule": "renders exactly the record_schema fields — nothing the player can't see is written on the card"
  },
  "draft": {
    "storage": "localStorage rw_create_draft_v27 — fields + step + resubmit count + landing pick + goes_by",
    "rules": "autosaves on every step render; discarded explicitly or cleared on successful hire; nothing leaves the browser in the demo"
  },
  "pending_queue": {
    "doc": "v63 — a submitted application keeps its place across a closed page. Pending state persists (rw_create_app_v27: fields + submitted timestamp + resubmit count + busId when filed live); reopening create.html shows the queue card (applicant, submitted time, stage, 'nothing charged while pending') instead of pretending the application vanished.",
    "withdraw": "a pending application can be withdrawn — never billed; the withdrawal posts to the feed like everything else and the draft survives for editing",
    "resolve": "the demo reviewer clears the queue on return; at merge the real human-review desk resolves it — the card is a status view, never a faster lane",
    "never": "no queue position is sold, no expedite exists, resubmissions go to a different reviewer not a faster one"
  },
  "day_one_keys": {
    "doc": "v63 — the keys card after signing: day-one logistics derived from the picks — key pickup (landlord's office for flats, door-on-the-latch + mailed key card for room shares), mailbox name card, first rent-book entry, first shift or the job-hunt runway, and the exact wire line that will post. Conditions and addresses only — never a script for the day.",
    "renders": ["KEYS", "MAILBOX", "RENT BOOK", "FIRST SHIFT", "THE WIRE"]
  },
  "sketch": {
    "doc": "v77 — the structured look picks draw a small canvas figure on step 2 and ride the review card on step 6 (drawSketch): silhouette proportions from build, jacket block from palette, one drawn mark per signature. Rendering data only — no new screening surface. Copy must call it a casting-office sketch, never a portrait."
  },
  "block_capacity": {
    "doc": "v91 — the total hired faces the block carries. The production envelope is pinned: GS_MAX_HIRED_TOTAL 24 (supersedes the v77 proposal 12). Per-account slot_cap stays a separate, smaller ceiling (GS_MAX_HIRED_PER_PLAYER 3).",
    "cap": 24,
    "production_pinned": true,
    "display": "the roster panel and the step-6 quote show the count verbatim ('N of 24 hired faces'); bridged, the count reads gsHiredRoster().length — never hidden, never a surprise denial"
  },
  "seat_waitlist": {
    "doc": "v77 — when the card is full, signing joins the seat waitlist instead of the review pipeline: free, first-come-first-served. Screened at join AND re-screened when a seat offer lands. An offer holds 48 h — unclaimed it passes on. Waiting never bills; leaving is free and posts to the feed like every withdrawal.",
    "storage": "localStorage rw_create_wait_v27 — fields + joined timestamp + resubmit count; survives a closed page like pending_queue",
    "never": "no expedite, no seat auction, no paid position — there is no way to pay for a sooner seat",
    "demo": "the demo seat opens on a clock (~40 s), the way the demo reviewer clears the queue; at merge the real roster state decides"
  },
  "repair_desk_v105": {
    "doc": "v105 — the desk's answer layer. A refused filing comes back as a returned application — a repair card, not a dead end: reason code verbatim, field-level 'fix the X' affordances that jump to the owning step (FIELD_FIX), draft never cleared. Returned state persists (rw_create_return_v105) exactly like pending_queue — a closed page reopens to the repair card.",
    "storage": "localStorage rw_create_return_v105 — {f, code, field, submits, at, busId}; cleared on resubmit, withdraw, or approval",
    "field_fix": {
      "name-collision/needs_name/name_*/name_collision → step 'arrival' (name/goes_by/age fields)": true,
      "underage/bad_age → arrival": true,
      "bad_look → appearance": true,
      "unknown_job/job_filled → work": true,
      "needs_housing/unknown_unit/unit_*/unit_occupied/out_of_reach → home": true,
      "engine text codes → the flagged screened field, defaulting to appearance (bio/arrival text)": true
    },
    "second_look": {
      "doc": "a text-judgment refusal gets ONE appeal — 'ask for a second look': the same words, a different reviewer, never faster, never guaranteed. A second refusal is final for that text (appeal_final — reword into a new application). Registry-fact refusals (taken names, occupied doors, caps, cooldowns, credit balance, engine faults) offer no second look — a different reviewer reads words, not the registry.",
      "live": "gsAppealRequest(busId) — the bus's appeal verb; a live refusal of the appeal renders its real reason code, never a fabricated one",
      "demo": "the demo desk re-reads on a clock and answers honestly — identical text gets the same refusal, final. It never fakes a reversal.",
      "never": "no expedite, no paid appeal, no third read on identical text"
    },
    "desk_status": {
      "doc": "a live filing parked in the naming lane resolves through gsExplainRequest instead of waiting blind — pulled on render and on 'check the desk' (pull, never poll): approved → the approval tail (the bus minted the hire; the page renders its mirror); denied → the repair bench; cancelled/expired/refunded → an honest closed line. The page still never fakes an approval on a live filing."
    }
  },
  "multi_hire": {
    "doc": "v77 — a second hired character is a stranger to the first, stated verbatim on the review step ('<name> is on the card too — a stranger to them; they meet on the block like anyone else') and in the briefing's surface-relationships line (a stranger, not a contact). What the player knows, neither character does — the briefing whitelist is unchanged."
  },
  "briefing_whitelist": ["public profile", "surface relationships", "daily routine"],
  "feed_events": {
    "request": "hire — '<name>' joined the cast (attributed, status resolved)",
    "housing": "move-in — <address> comes off the board",
    "cast": "a new face on the block — <name>, <public bio one-liner> (bio text only post-screen)"
  },
  "progression_hooks": [
    "tenant → owner: purchase listings use the same 9xxx addresses; deed fee in credits after the earned game-dollar grind",
    "owner → landlord: landlord license gates on earned in-world standing"
  ]
}
