# SF Mission build — progress checkpoint

Branch: `sf/full-neighborhood` (from `anim/28-cast`), local only.

## Done
- Phase 1: `scripts/convert_sf_osm.py` merges rw-sf-spike/osm_raw/part{1,2}.osm
  -> `src/data/sf_map.js` (771x813 cells, 2m/cell, 5837 buildings w/ real
  heights, 800 POIs, 324 named roads, park/props). Cartoon art in
  `src/sf/31_sf_art.js` (pa-* primitives: asphalt+crosswalks+sidewalk curbs,
  park grass/paths, blob trees/palms/benches/lamps, Victorian facade compiler
  `sfBldCanvas` w/ cornice, capsule windows, bay windows, doors, awnings,
  painted signage, textured roofs). Chunks via `sfGenChunk`.
- Phase 2: `sfCanMoveTo` (buildings block, doors walkable), A* `sfPathfind`
  (closed-set, raw mode for movement) + `sfFollowPath`/`sfGoTo`, `sfFindPOI`,
  `sfPoiDoor`, `sfEnterPOI`/`sfExitPOI` door-teleport interiors for Haus
  Coffee, El Farolito, Auerbach, 744/750 Guerrero, Dolores Park anchors.
- Phase 3: `src/sf/33_sf_cast.js` — NV_CAST C1..C8 re-skinned per cast bible
  (pa-chars.js), routines for core 8 + role-driven thin schedules for 20
  ambients, `sfNpcTick` dispatched from `updateVillagerAI`.
- Phase 4: `src/sf/34_sf_live.js` — Open-Meteo for 37.7596,-122.4269 +
  America/Los_Angeles clock sync; gated behind SF_MODE (must early-return
  when !SF_MODE — it broke save22 determinism otherwise).
- Activation: `?sf=1` or `?scenario=sf`; `?view=street` for Truman camera
  (also V key). `?inspect=<name>` focuses a cast member.
- Harnesses: `devtools/sf_harness.js` 17/17, `node_harness.js` 0 fails,
  `anim_harness.js` 2034/0.
- Screenshots: rw-sf-spike/sf_top.png, sf_street.png via Playwright.

## Interfaces
- `SF_MODE` const (30_sf_world.js), `SF_VIEW` (32_sf_render.js)
- Dispatch: genChunk, canMoveTo, updateVillagerAI, renderWorld, boot.
- Movement pathfinding uses `sfPathfind(..., raw=true)` (unsmoothed —
  smoothing lets the 8px body radius clip wall corners).

## Remaining
- Final report + commit. Converter demotion pass for false crosswalks is in
  place; if zebra bands reappear on a new extract, tune the run16>9 rule.
