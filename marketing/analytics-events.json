{
  "$schema": "internal/real-world-analytics-events/v1",
  "comment": "Machine-readable event spec for Real World marketing + funnel analytics. Client emitter: marketing/site/js/analytics.js. Dev sink: marketing/tools/analytics_sink.py. See ANALYTICS.md for the full plan. All events are anonymous (session nonce only, no persistent ids, no cookies).",
  "envelope": {
    "v": "int — schema version, currently 1",
    "site": "string — site key, default 'realworld'",
    "event": "string — one of the names below",
    "path": "string — location.pathname, e.g. '/pricing.html'",
    "props": "object — per-event properties, see events",
    "sid": "string — random session nonce from sessionStorage; dies with the tab; NOT a visitor id",
    "utm": "object — utm_source/medium/campaign/content/term + ref, captured at landing, session-scoped",
    "ref": "string|null — referrer HOST only, never the full URL",
    "ts": "int — epoch milliseconds"
  },
  "events": {
    "pageview": {
      "when": "once per page load, auto",
      "props": {
        "title": "document.title",
        "page": "body[data-page] slug: index|features|how-it-works|demo|cast|journal|community|rules|pricing|faq|press-kit|404",
        "vw": "viewport width (for layout QA)",
        "lang": "navigator.language"
      },
      "funnel_stage": "visit"
    },
    "cta_click": {
      "when": "click on any element carrying data-rw-event=\"cta_click\"",
      "props": {
        "cta": "slot name: hero|walkthrough|footer|nav|pricing-teaser|faq-exit|404-exit",
        "dest": "logical destination: how-it-works|features|pricing|faq|index",
        "href": "raw href (same-site)"
      },
      "funnel_stage": "visit → engaged"
    },
    "screenshot_view": {
      "when": "click on a .gallery img (opens lightbox)",
      "props": {
        "shot": "image src basename",
        "alt": "alt text, truncated 80 chars"
      },
      "funnel_stage": "engaged"
    },
    "outbound_click": {
      "when": "click on an external link not otherwise instrumented",
      "props": { "href": "full outbound URL" },
      "funnel_stage": "exit"
    },
    "scroll_depth": {
      "when": "page scroll crosses 25/50/75/100% — fires once per mark per page (v21)",
      "props": { "depth": "25|50|75|100", "page": "data-page slug" },
      "funnel_stage": "engaged"
    },
    "engaged_time": {
      "when": "pagehide — total seconds the page was actually visible (v21)",
      "props": { "seconds": "int, visible time only", "page": "data-page slug" },
      "funnel_stage": "engaged"
    },
    "share_click": {
      "when": "demo.html share button completes (v21)",
      "props": { "method": "web-share|clipboard|manual" },
      "funnel_stage": "engaged → viral"
    },
    "price_calc": {
      "when": "pricing.html estimator control changes, debounced 900ms (v22)",
      "props": {
        "class": "compatible|exclusive",
        "minutes": "int — duration priced",
        "queued": "bool — −15% toggle on",
        "surge": "bool — surge-window preview on"
      },
      "funnel_stage": "engaged → purchase intent",
      "note": "carries NO amounts — only which knobs visitors touch; reveals demand for class/duration before checkout exists"
    },
    "press_kit_download": {
      "when": "click on the kit zip link (data-rw-event on press-kit.html)",
      "props": { "file": "zip filename" },
      "funnel_stage": "press"
    },
    "watch_start": {
      "when": "spectator feed first rendered (emitted by the game embed, not the marketing shim; spec'd here for the funnel)",
      "props": { "source": "demo-page|direct|embed" },
      "funnel_stage": "watch",
      "status": "PENDING — depends on game build / demo page (roadmap v12)"
    },
    "request_submitted": {
      "when": "a viewer files a world request (game-side event; spec only)",
      "props": { "class": "compatible|exclusive|queued", "credits": "int", "duration_min": "int" },
      "funnel_stage": "request",
      "status": "PENDING — game build"
    },
    "character_created": {
      "when": "a viewer hires/creates a character (game-side event; spec only)",
      "props": { "source": "cta|settings|post-request" },
      "funnel_stage": "create",
      "status": "PENDING — game build"
    }
  },
  "privacy": {
    "cookies": false,
    "persistent_ids": false,
    "ip_storage": "server-side daily-rotating hash only (salt + IP + UA + date); raw IPs never stored",
    "opt_out": ["navigator.doNotTrack", "navigator.globalPrivacyControl", "?nocollect=1", "localStorage rw:no-collect=1", "rw.optOut()"],
    "inert_by_default": true
  }
}
