# Willowbrook Natura — `src/` module manifest

The game is authored as plain-JS modules under `src/`. The bundler
(`scripts/build_willowbrook_natura.py`) concatenates them **in the order listed
in `src/_order.txt`** into one `<script>` block — no ES modules in the output,
so the page stays openable from `file://`.

**All modules share one script scope** (as before the split). Treat top-level
`const`/`let` names as global: do not redeclare a name in two modules, and do
not reorder modules without checking cross-module references — the order IS the onion: outer data/entities layers are visible to inner systems layers, and modules may call any function declared in the same script scope. Within those
constraints, each module is an independent unit of the simulation.

## Module map (bundle order)

### `sim/` — world, time, space
| module | from | contents |
|---|---|---|
| `sim/00_core.js` | PART 2 | constants (CS/CHN), world clock & weather state `W`, noise/math helpers, chunked terrain gen, system registry |
| `sim/01_settlement.js` | PART 3 | village layout: `VILLAGE_BUILDINGS`, `VILLAGE_OBJECTS`, `initVillageSettlement` |
| `sim/04_navigation.js` | PART 5B | movement, water depth, collision |
| `sim/06_mainloop.js` | PART 7 | main loop, `simTick`, time advance |
| `sim/11_boot.js` | PART 11b | `boot()`, canvas setup, art prerender, `?test` dispatch |
| `sim/12a_events.js` | PART 12A | event log, mortality, `survivalGuard`; 2B: river `placePos` at bridge |
| `sim/12d_parity.js` | PART 12D intro | parity world-state arrays (PILES/FIRES/CROPS/CHICKENS/BURNING), `initParityWorld` |
| `sim/12d_world.js` | PART 12D | crops, chickens, campfires, lightning, wildfire, regrowth ticks |

### `entities/` — things that live
| module | from | contents |
|---|---|---|
| `entities/02_body.js` | PART 4 | living bodies: needs, biometrics, `bodyTick`; Phase 2E: hygiene need (0..1), elder metabolic modifiers (1.35x fatigue, 0.85x calorie burn), hygiene sensations & thoughts; Phase 6C: `bodyWorkFactor` & `bodySpeedFactor` chain (`calcBodyFactors`), emotion derivation layer (`updateDerivedEmotions`: hungry→anxious, pain→suffering, stable→content + decay) |
| `entities/12d_pregnancy.js` | PART 12D | pregnancy, birth, children growth; Phase 2E: `birthChild` reuses `createVillager` factory, records parentage (`motherId`/`fatherId`), household membership, 2D parent and witness birth memories; pregnancy cooldown (30 sim-days), deterministic aging hook. 2E FIX: conception is married-only (unmarried fallback removed), adult-stage conscious non-starving females only; ALL randomness seeded via `hashString18(SEED:…)` — zero `Math.random` (name/sex/seed/conception draw). |
| `entities/14c_wildlife.js` | PART 14C | wolves/boars/bears AI, `hunt` verb, carcasses |
| `entities/15b_husbandry.js` | PART 15B | taming, pens/coops, feeding, breeding |

### `systems/` — gameplay systems (verbs & mechanics)
| module | from | contents |
|---|---|---|
| `systems/12c_actions.js` | PART 12C | action queue, `postAction`, 16+ verbs, multi-step plans; Phase 2E: child weaker carry capacity (max 6), elder slower movement (0.75x speed). 2E FIX: `planMoveToward` hardened against non-finite targets (returns 'stuck', never corrupts position); planTick `'go'` resolves `person`→coords when no `follow`/coords given (fixes socialize/teach NaN zombie bug); `occupationBonusFor(v, act)` helper wires the +25% occupation bonus into farm/cook/fish executors (progress rate); Phase 6C: Survival Guard with death-threshold triggers (thirst<8, hunger<6, warmth<6, blood<70, adjacent fire/wolf) overriding interruptible actions + anti-oscillation lock (`v.guardLock = 10`), emergency/survival actions never interrupted |
| `systems/12d_social.js` | PART 12D | bonds, autonomous chats, chat lines |
| `systems/12d_illness.js` | PART 12D | base illness & injury tick; Phase 6C: untreated wounds feed into infection/illness progression, updates `calcBodyFactors` |
| `systems/13b_economy.js` | PART 13B | gold, Sella's shop, buy/sell; 2B: inn trade (Tobin serves meals) |
| `systems/13c_butcher.js` | PART 13C | butchering, corpse decay, burial, wildfire building damage |
| `systems/14a_medical.js` | PART 14A | wounds/bleeding/infection, poultice/splint/fever-tea, `tend` |
| `systems/14b_rescue.js` | PART 14B | downed states, `rescue` carry |
| `systems/14d_skills.js` | PART 14D | 8 skills, XP, talents, `assignWork` |
| `systems/14e_construction.js` | PART 14E | materials, integrity, `repair`, fences/coops/huts; 2B: `CONSTRUCT` entries for barn/workshop/kitchen |
| `systems/14f_wiring.js` | PART 14F | PART 14 verb registration, planTick cases, `boot` override |
| `systems/15a_food.js` | PART 15A | meal tiers, spoilage, food poisoning, `preserve` |
| `systems/15c_caravan.js` | PART 15C | seasonal merchant caravan, `trade` |
| `systems/15d_friction.js` | PART 15D | insults, grudges, rivalries, brawls, apologies |
| `systems/15e_firefight.js` | PART 15E | `douse`, autonomous firefighting duty |
| `systems/15f_wiring.js` | PART 15F | PART 15 verb registration, planTick cases |
| `systems/16a_recipes.js` | Phase 2A | generic `recipe` verb: runs any `RECIPE_TABLE` entry (inputs/tools/skill checks, workstation walk, provenance on outputs); `planForVerb`/`planTick` wraps; intent patterns (saw/mill/bake/build). Workstation policy: station missing -> work in place; station exists but unreachable (pathing `stuck`) -> thought "Can't reach the X" (val -2) and the step ends — never silent. 2E FIX: crafting progress rate multiplied by `occupationBonusFor(v, 'crafting')` (carpenter +25%). |
| `systems/17a_buildings.js` | Phase 2B | building entity model: indoor temperature dynamics (fire warms, winter cools), cleanliness 0..1 decay & cleaning (`clean` verb), capacity & overcrowding mood penalties, ownership tracking (`actualOwner` precursor), expansion (`expand` verb costs logs/stone, adds capacity), abandonment decay (unoccupied N days -> fast cleanliness decay + slow structural decay), demolition (`demolish` verb reclaims 50% materials). Functional buildings: barn (animal housing & shelter), workshop (workbench workstation for recipes), kitchen (cooking station with fire), inn (guest rooms & meals commerce with Tobin). Procedural pixel-art prerender for barn, workshop, kitchen (`composeBuilding`). |
| `systems/20_social_life.js` | Phase 2E | Social life engine: life stages (child->youth->adult->elder) with heavy verb blocking for children (fell, construct, demolish) and heaviest work for elders (fell, demolish, expand); marriage mechanics (2 conscious adults, mutual bond >= 0.7, proximity <= 4 cells, spouseId links, wedding memories, witness relationship beliefs, honest spouse cleanup on death); household entity system (shared home building, household membership, home storage food preference); ordinary activities (bathe restores hygiene with cold/dirt risk, visit builds bond, play lowers stress and boosts mood, childcare feeds child from REAL pooled household food stock — conservation enforced, honest failure thought when stores empty — and hydrates only near real water sources, insult, sweep/tidy clean); relationship bond decay without contact (~0.01/day); dynamic businesses (productive activity tracking for farming/crafting/cooking/fishing/trading, occupation emergence with 25% efficiency bonus WIRED into farm/cook/fish/craft progress rates and trader stall-price discount, village market stall entity for commerce); utility AI candidate & scoring integration (life stage, hygiene deficit, childcare, visit, play, occupation bonus, home food); natural language intent parsing. 2E FIX: failed 'marry' plan records explanatory thought + observation instead of vanishing silently. |
| `systems/21a_ownership.js` | Phase 2F | Ownership, provenance & information layers. IDENTITY BOUNDARY (documented): identified kinds (plank, furniture — stable id, creator, quality, condition, material parentIds, tree lineage roots, full event history) live in the ITEMS registry AND keep a count mirror in v.inv[kind] so legacy count semantics keep working; bulk goods (food, grain, logs, crops) stay count-based with no identity. OWNERSHIP LAYERS: actualOwner (world truth, only legitimate transfer changes it) vs currentHolder (possession) vs per-character knownOwner/suspectedOwner/confidence/evidence/claims via Phase 2D epistemic APIs — actualOwner never broadcast. Transfer engine recordTransfer with exact semantics: gift/sell/buy/inherit change holder+owner; steal changes holder only (stolenFrom set); borrow changes holder, keeps owner, records expectedReturnH; return restores; lose/find/abandon keep owner; claims never alter actualOwner. Every transfer appends to the item's immutable history AND the world transfer log; participants and conscious witnesses form memories/beliefs through 2D APIs. Material lineage: stable tree ids (ensureTreeId/recordFelledTree), getLineage(itemId) returns chair->plank->tree chains; crafting identified outputs stores parentIds + treeIds (anonymous fallback for legacy bulk inputs). Claims/disputes: raiseItemClaim records claim + witnesses (2D claimOwnership untouched); conflicting claims open a dispute; resolveDispute is deterministic (evidence ladder: creation/direct observation > witnessed transactions/possession history > hearsay > bare claim; thief admission counts against them), mayor (Alden) decides else oldest adult non-claimant else holder keeps pending; mediation orders POSSESSION only, never rewrites actualOwner; resolution recorded as permanent event; loser gets memory + bond grudge. Verbs: steal (conscious-witness check, caught leaves item in place with beliefs/bonds/memories, unseen moves holder only), borrow (bond-gated request, honest refusal), giveback, mediate; take/drop of identified kinds go through the registry (placed/unattended/lost/abandoned pickup via 'find'); taking another's HELD item is honestly refused, never silent. ownershipTick: dead holders set items down, missing-item notices (last event steal/find), overdue-borrow notices. Utility: giveback (overdue first), recover own items, mediate, borrow-from-friends, hard-gated steal candidates; scoring consults beliefs, never world truth. No Math.random; no mood engine. |
| `systems/29a_court.js` | Phase 7A | Customary court: institutional hearing replacing automatic proto-norm punishment. Selects presiding elder (eldest adult non-party living villager, tie-broken by social reputation); gathers witnesses via local belief check (only villagers with sensory presence or un-superseded epistemic memories); collects belief tuples {claim, suspect, source, confidence, fidelity}; elder deliberates based on testimony credibility vs defense plea; verdicts: restitution (physical transfer of item/gold preserving material conservation), public labor, exile (ostracism); records decaying court-verdict reputation reasons; generates long-decay grievance and downstream retaliation upon wrongful conviction (belief!=truth). |
| `systems/29b_guilds.js` | Phase 7A | Trade guilds (smith, baker, farmer): membership registry and ranks (apprentice, journeyman, master). Apprenticeship: learning with a guild master provides deterministic, measurable XP gain bonus (1.60x) in gainXP; outsiders receive zero bonus. Guild bloc trading: selling bulk lots (>=10 units, e.g. 10 bread) to traveling caravans fetches unit price > retail piecemeal sales; outsiders selling bulk receive standard retail unit price only. Absolute material conservation maintained. |
| `systems/41_game_systems_registry.js` | game v0/v4 | "The Mission" address/building/unit/lease registry (rw-address-system-spec.md): deterministic 9xxx numbers `gsAddrNumber` (9000 + hash(street\|idx) % 900, collision-bumped), in-memory `GS_REG` + derived indexes, lease lifecycle (sign/end/evict, address immutable across turnover), clerk minting `gsMintAddress` + mintLog, retired numbers never reused, JSON snapshot/load, `gsSeedSF` SF_MAP auto-seed under SF_MODE incl. canonical cast homes (Jules deliberately off-lease). **v4:** unit codes are spec-format, unique per building forever (retired codes never recycle), auto-assignable (`gsNextUnitCode`, A–Z then A1–Z9); `gsUnitLivable`/`gsUnitVacant`/`gsUnitClaimed` guard every placement path; spec §6 clerk ops — `gsSubdivideUnit` (vacant unit -> n flats, same address, `A`->`A1`/`A2` codes), `gsConvertGarage` (one ADU per lot, 'G' code, not rent-controlled), `gsMergeUnits`, `gsRetireUnit`, `gsRetireBuilding` (refuses occupied unless forced) — all register-then-log to mintLog + public feed; seed uses REAL unit counts (real house-number list count, footprint-area fallback — real numbers never stored). `hnPin` designer pins land canonical numbers formula-legally (search `gsAddrNumber` -> `addr_idx`, stripped from clerk mints); `canon`/`offmap` tags mark authored homes (Geneva/Folsom homes are real places off the rendered map). |
| `systems/41_game_systems_ledger.js` | game v0/v1 | Two-currency ledger `GS_LEDGER` (design §6): credits (player agency, 'p:*' accounts) + game dollars (rent/wages, entity accounts) that never mix. Append-only txn log, integer units, conservation, insufficient-funds atomic no-op, low-credit threshold, `gsCollectRent` tenant->owner via active lease (unit-level condo title `u.owner_id` wins over building `owner_id`), snapshot/load. |
| `systems/41_game_systems_requests.js` | game v1/v2 | Request bus `GS_REQ` (design §5) made real: catalogue of possess/weather/street_event/hire/listing/buy with honest per-minute rate card; effect dispatch (maintained activate/deactivate + once-effects) — possession suspends a hired character's AI (`GS_POSSESS` + `isNPC` flip), weather overrides the sky (`GS_WX_OVR` applied post live-weather each frame), street events register `GS_EVENTS`, hire/rehouse delegate to the personnel office (41H), listing/buy run real title transfers through the ledger. Upfront hard-cap billing + per-minute metering: player cancel refunds whole unused minutes, admin revoke always full-refunds (`gsAdminRevoke` posts 'admin' feed lines). **v2 pairwise conflict matrix**: every request declares claims (`char:*`, `sky`, `venue:*`, `openair`, `paper:*`, `listing:*`); `gsClaimClash` decides pairwise conflicts (same class+resource clashes; identical weather forecasts co-sponsor via `GS_WX_OVR.sponsors`; severe weather × outdoor events clash both directions). Global-FCFS `gsPromoteAll` promotes in submission order with no leapfrogging and re-validates stale requests (fail + full refund); cancel/revoke/expiry/completion all trigger promotion. Compatible multiplayer is surfaced via `session` feed events + `gsCoSessions`. Viewer APIs: `gsQueuePosition`, `gsRequestMeter`, `gsViewerState` (sessions + blockedBy), `gsPossessionBriefing` (public fields only), `gsExplainRequest`, `gsConflictRules`. `gsBusTick` wired via `registerSimTick` on real wall-clock minutes. Snapshot/load covers live fx state incl. sponsors. v4: hire/listing `allow()` reject off-map units (`unit_offmap`) and non-livable units before billing. **v5:** §11.3/§11.4 request pipeline — `gsIntentScreen` runs on every filing's authored text before billing; deny-tier codes (world/moderation.json vocabulary) refuse without charging, review-tier hits park the request `in_review` (billed upfront, `gsReviewResolve` enters the line at approval-time with re-validated claims or refunds in full on denial; `GS_REVIEW_TTL_MIN` lapse auto-refunds). Possession deny vocabulary upgraded via `gsPossessDeny` (`possession_ban`/`cast_ai_only`/`not_your_character`); `gsFxPossessOn/Off` delegate to the 41F session layer; `gsBusTick` ends on `gsPossessTick`; hire activates `gsSpawnHired`; `gsViewerState` exposes `review` + `driving`; snapshot carries the driving record (`plog`). **v14 THE BOOK** (world/bookings.json live seam): weather + street_event filings may declare a start window (`start_slot` minutes-from-now or absolute `startMin`, snapped up to the 30-min grid, next-24h horizon) — approval lands them on the public calendar as status `booked` (`gsViewerState().calendar`, `gsBookCalendar`, `gsBookableSlots` picker); `gsBusTick` fires a booking when its window arrives (never early, a running lock holds it, a fully-missed window expires with full refund); conflict math is window-aware (`gsReqWindow`/`gsWindowsOverlap`) so booked spans claim their resource for their interval; `gsBookTailDeny` refuses slots inside cooldown/venue-rest tails at the door AND in the quote; FCFS sliding (`gsBookNextFree`) moves a blocked booking to the soonest free legal slot — never an auction; the clip rule lets a queued ask run until a booked window opens (`clipEndMin`, un-run minutes refund at completion); pre-window cancel refunds in full; surge keys off the window's local hour; bookings never take the queue discount; `gsRequestMeter`/`gsExplainRequest`/`gsConflictRules` speak booked state; bridge adds `gsBookCalendar`/`gsBookableSlots`. **v15 THE MUNICIPAL CODE** (with 41P civic): event claims delegate to `gsEventClaims` (zoned venue res + `noise:<place>` airspace for amplified kinds); `gsVenueResTouch` drives zone/whole-place/noise clash math; `allow()` takes `durationMin` everywhere so quiet-hours is judged on the real run window; `gsCivicHoldDeny` refuses at the door, `gsLiveHolds` parks the queue, `gsFireBooking` never runs into a hold; co-hosting (`gsCoSponsorLive`/`gsCoSponsor`) merges identical overlapping events into one `GS_EVENTS` record with per-sponsor windows — the party ends when the LAST permit ends; `fitness_class` joins the event kinds; `gsBusTick` ends on `gsCivicTick`; `gsViewerState` exposes `holds`; snapshot carries `civic`. |
| `systems/41_game_systems_leases.js` | game v3 | Lease lifecycle `GS_LEASE` (roadmap v3): applications (`gsApplyForLease`/`gsApproveApplication`/`gsDenyApplication` — approval signs + posts the deposit charge), per-occupant rent shares with pay habits (`lateEvery`/`lateDays` — Marcus's every-third-month lateness is real mechanics), the daily `gsRentTick` (monthly charges on `dueDay`, pro-rated first months, auto-pay, 5-day grace, 5%-capped late fees, arrears per occupant via `owedBy`), CA-style notices (`pay_or_quit`/`cure_or_quit` need arrears or a DISCOVERED violation — inspections/reports surface them, undiscovered violations never hit the public feed), admin-only `gsAdminEvict` (executable notice or owner override — overrides post `noFault`), tenant `gsVacate`, rent raises with SF-ordinance caps (7% controlled / 10% not, once per 12 months, 30-day notice), fixed-term → month-to-month rollover. `gsHomeOf`/`gsCastHousing`/`gsResidentsOf` resolve who lives where (tenant/occupant/unpermitted — Jules resolves through her violation record). `gsLeaseSeedSF` houses all 20 ambients in nearest real buildings + seeds bank balances. v4: `disputes` on leases (open rent-raise dispute on the contested 2600->3200 raise at 9457-3), unpermitted-occupant violations for Jules and Dani's room-share, Reyes cousins' household + balance, ambient family at 9457-1. `gsLeaseSysTick` runs one rent run per real SF day. |
| `systems/41_game_systems_addressing.js` | game v4 | Addressing layer — the clerk's front desk (read/resolve/audit; mutating clerk ops live in the registry). Polymorphic lookups for AIs: `gsAddressOf` (unit or building -> address), `gsResidents` (unit or building -> residents incl. unpermitted), `gsOccupancyOf` (per-unit clipboard view), `gsVacantUnits` (leasable stock for hire/listings), `gsParseAddress` ("9431 Guerrero Street, San Francisco, CA Unit B" + loose forms -> records), `gsLookupPlace` (one-call resolution by id or address string), `gsRegistryStats`. `gsRegistryAudit` verifies the invariants: §7 privacy (all numbers >= 9000), §3 formula conformance, per-street uniqueness, address immutability, unit-code uniqueness, orphan/lease coherence. `gsCastAddressBook` + `gsBibleMigrationReport` execute spec §8 — every cast member's bible HOUSEHOLD resolves to its canonical world-content address (exact match via `gsAddrMatches`; off-map homes flagged `offmap`). `gsVacantUnits` excludes off-map stock unless asked. All bridged on `__aiBridge`. |
| `systems/41_game_systems_possession.js` | game v5 | Possession session layer + hired-cast bodies (design §7 + §11.3/§11.4). `gsPossessDeny`/`gsPossessGate` own the eligibility vocabulary (core ban absolute, ambient `cast_ai_only`, own-hire only). `gsSpawnHired`/`gsDespawnHired` give hired characters real pawns: deterministic `gsHiredLook` through the pa-chars design/frameset pipeline (no Math.random), `gsHiredRoutine` thin schedule anchored to the real lease door, `gsHiredHomeCell`/`gsCellToLatLon` lease→door resolution. Sessions: `gsPossessOn`/`gsPossessOff` write the driving record `GS_POSSESS_LOG` (start/end cells, minutes used, dollars spent, `rentRisk`, endReason timeout/released/admin_revoked/orphan_sweep); `gsPossessTick` emits the one-shot 5-min `winddown` warning and sweeps orphaned sessions (no ghost driving). Driver verbs: `gsPossessDrive` (click-move waypoints through player-pawn physics, POI-exit on entry) and `gsPossessPay` (character's own game dollars via `gsDollarPay`, landlord payments settle through `gsPayRent` — credits never touch it). `gsPossessBrief` is the full public-record packet (profile, paperwork relationships, routine, wallet, body, rules + `redacted` list); `gsBriefingAudit` proves no banned key surface leaks. `gsIntentScreen` is the §11.3 classifier with canonical `world/moderation.json` codes (deny + review tiers) and `gsScreenHistory` repeat-pattern/appeal signals. Viewer APIs: `gsPossessionSession`, `gsPossessDriving`, `gsPossessLog`, `gsPossessDebrief`, `gsHiredRoster`. `gsPossessSnapshot`/`gsPossessLoad`/`gsPossessReset` ride the bus snapshot. All bridged on `__aiBridge`. |
| `systems/41_game_systems_hiring.js` | game v8 | THE PERSONNEL OFFICE — the hire pipeline as a real application (world/creation.json + jobs.json + shifts.json). Structured application fields (name/age/pronouns/bio/arrival/look{build,palette,signature}/job_id/unit_id); `gsHireNameCheck` is the live name-availability registry check (create.html TAKEN list + every born hire) and `gsHireAppCheck` the structural gate (needs_name/name_collision/underage/bad_look/unknown_job). Flat 500cr via `billOnApproval` — the charge lands at review approval, never at filing, so denied applications never bill. `gsHireAllow` orders housing -> caps -> fields -> job -> the 55% rent-to-income ceiling (`out_of_reach` at pick time; 'seeking' stays legal with the runwayMonths number on the record). `gsHireActivate` allocates monotonic `h##` ids, signs through the ordinary application path with the deposit waived (`hirePackage` flag on the lease), grants the $1,600 arrival bank, posts the pro-rated first month and consumes the job opening atomically. `GS_JOBS` mirrors the canonical openings with finite slots; `gsJobTick` posts thin-AI wages employer->character every Friday (weekly entry/informal, biweekly mid/top per shifts.json) with a small session bonus while possessed; employers carry an honest operating float. `gsHiredJobRoutine` builds the sfSched day from the posted shift; `gsHiredTakeJob` lets a 'seeking' hire take work later; the 'rehouse' request re-doors an evicted hire through normal paperwork (deposit owed — a real second lease). `gsHireQuote`/`gsJobBoard`/`gsHireSlots` are the pre-payment disclosure card + board + slot counter; `gsHireSnapshot`/`Load`/`Reset` ride the bus snapshot. |
| `systems/41_game_systems_feed.js` | game v6 | THE WIRE — the public spectator feed (world/feed.json + history.json). A formatted projection of `GS_FEED`, not a second log: `gsWireFormat` maps every bus event to 0..n display-safe entries (`{id,t,day,kind,venue,who,text,status,reason_code,attrs,mentions}`) with stable citeable ids (`d<MMDD>-<feedN>`), canonical request statuses (`running`/`queued`/`in_review`/`approved`/`resolved`/`refunded`/`not approved`/`player session ended`), and verbatim contract wording ("request not approved", "admin action — tenancy ended at <address>"). Privacy is structural: denied request text never enters an entry; lease payments/arrears/reasons are drop-policy suppressed and counted for audit; eviction/violation lines carry no amounts or names. `GS_WIRE_REASON` maps internal denies to moderation.json codes (possess bans -> `possession-scope`). Display filter modes A/B/C (redact spans / withhold notes / quarantine) govern player-authored note suffixes; `GS_WIRE_PARODY` rewrites real business names. Reads: `gsWire` (kind/status/venue/who/mention/since/before/followedOnly filters), `gsWireTail`/`gsWireSince`/`gsWirePage`, `gsWireFollow` pins, `gsWireStats`, `gsWireVocabulary`, `gsWireAudit` (schema + monotonic ordering + denied-text leak grep + money-in-text ban + suppression reconciliation), `gsWireDays`/`gsWireArchiveDay`/`gsWireArchive` (history-shaped day objects reusing live ids). `gsWireTick` (bus beat) emits the honest `quiet` marker after a silent stretch. `gsWireShow` lazily builds the plain read-only `#gs-wire` DOM panel — auto-on for `?sf` outside `?test`, `?wire=0/1` overrides. `gsViewerState().feed` is now wire-formatted (`feedRaw` keeps the raw rows); `gsBusSnapshot` carries `wire`; `gsBusLoad`/`gsBusReset` restore/rebuild it. All bridged on `__aiBridge`. v14: booking wire vocabulary per world/bookings.json copy deck — 'approved · booked for HH:MM' on landing, 'booked window arrived — <action> fired' on fire (new `fire` event kind), 'cancelled before the window, refunded', 'window missed, fully refunded', queued bookings read 'in line for HH:MM', clipped wraps read 'wrapped early · un-run minutes refunded'; player-called weather prints on the weather channel at FIRE time, never at filing; no new status chip — booking is a schedule, not a state. |
| `systems/41_game_systems_antigrief.js` | game v7 | THE DOOR POLICY — the anti-grief account layer (world/moderation.json + requests.json) in front of the bus. Account flags with the contract's verbatim `flag_weights`: deny verdicts accrue score, the ladder routes every filing through human review at 3, suspends request privileges 72h at 6, owner-holds at 9, and decays -1 per clean 30 days (`gsFlagBump`/`gsAccountGate`/`gsFlagClear`). Rate caps: 20 filings/h + 8 live per player, pre-billing (`gsRateCheck`). Appeals: one per denied request within 72h, free to file, re-bills on approval, different-reviewer enforced, second denial final per request TEXT (`gsAppealRequest`/`gsAppealFinal`/`gsFinalText`, aggregate-only feed visibility). `gsEscalateLegal` is the legal kill switch — kills queued/active/review requests, full 'legal-deny' refund, public wording identical to any denial. `gsWatchAd` mints the capped trickle (2cr/view, 5/PT-day). `gsRepNote`/`gsRepLedger`/`gsRepScore` are the internal reputation notebook (the v11 hook). `gsResourceBoard` projects claims + venue-rest cooldowns into free/cool/locked/queued. `gsPriceQuote` is the pre-payment receipt — surge factor, primetime, queue discount, lane, refund terms, nothing mutates. `gsModMetrics` is the owner dashboard (review depth, oldest wait, median decision, denials by code, appeal reversal, compensated credits). One bus hook `gsGriefOnEvent` feeds flags + notebook off the same feed events; `gsGriefSnapshot`/`gsGriefLoad`/`gsGriefReset` ride the bus snapshot. All bridged on `__aiBridge`. v14: `booked` counts against the live-request cap (a calendar is not a loophole) and is reachable by `gsEscalateLegal` (full refund); `gsResourceBoard` shows claimed future windows under a `booked[]` marker per resource (free-now + promised-later); `gsPriceQuote` normalizes `start_slot`/`startMin` with the door's own codes, refuses tail-inside slots, keys surge + `primetime` off the window's local hour, returns `wouldBook`/`booked{startMin,start,day}` + the booking refund note — a booking never quotes a queue discount. |
| `systems/41_game_systems_offline.js` | game v9 | THE QUIET HOURS — offline-mode + brain-mode plumbing (roadmap v9 + world/thinai.json). Presence layer `GS_PRESENCE` (`gsPlayerOnline`/`gsPlayerOffline`/`gsPresenceOf`/`gsPresenceSeen` — filing a request is presence; disconnect ends live driving FIRST via the early-release path, then starts the 90s linger so blips never drop a brain). Brain modes are DERIVED, never stored: `gsBrainMode` = 'possessed' | 'thin' (ambient + hires whose owner is offline) | 'full' | 'degraded'. Handoff notes `GS_NOTES` — every mode swap writes the contract's one-paragraph page ({char, from, to, at_min, place, doing, pending, near, mood_hint, transition: offline_drop|wake|handoff|degrade|recover}), read once, stale after 24h; possession release + orphan sweep carry the "I was just moving through here — " lead. Seam rule `GS_SEAM` queues transitions until a pawn finishes its micro-beat (<=90s). Degrade ladder `gsServiceSet`/`gsServiceEval` — capacity <60% degrades mains lowest scene-salience first, <30% all of them. Needs model `GS_NEEDS` — hunger/rest posture over the routine; `gsThinScheduleView` is the schedule-as-read (hunger detour, early night, co-star override — the raw `sfSched` never mutates). Phrase kit `gsThinSay` — <=3 generic lines/hr, shared set, no char voice. CO-STAR mode: new bus action `costar` (2cr/min, 5-60min) summons a thin pawn for a bounded stint — `gsCoStarCheck` role-card fit + reflex vetoes (resting/on_shift/no_event/needs_first/worn_out) at activation; a decline resolves 'resolved · declined' on the wire with half the bill back. `gsPossessDeny` gains 'owner_offline' — a queued possess can't promote into a player who isn't there. `GS_COMPUTE` honest brain-minute ledger (thin/possessed/degraded never tick the LLM meter). `gsObligations` proves rent/wages/jobs survive every drop. `gsOffTick` drains lingers/seams/ladder/needs/notes; `gsOffSnapshot`/`Load`/`Reset` ride the bus snapshot. Transitions never emit feed events — a spectator can't tell a brain dropped. All bridged on `__aiBridge`. |
| `systems/41_game_systems_listings.js` | game v10 | THE DEED OFFICE — in-world real-estate listings for the tenant→owner arc (roadmap v10, world/market.json + lease-ui.md + monetization §2.4). Listing records gain `kind` (sale|rent), `quiet` (pocket listings — feed-visible only when public, the block finds out when it sells), `carriesNote` (seller financing), generated `copy` (honest strength + honest flaw + parody-only venues; never the address-generation rule) + screened `sellerLine`. `GS_BLD_LISTINGS` holds whole-building sale cards; `gsListingCheck` shares validation between the request lane and the clerk's desk (`gsListUnit`/`gsQuietShop`/`gsDelist` admin tools). `gsListingBuy` runs the real escrow — offer at asking, no bidding; deed fee in credits by ladder tier (1000/2000/3500/5000cr); seller-carried note math (`gsMortgageMath`, 6.8% fixed, ≥20% down); the seller's own note pays off out of proceeds (`seller_underwater` refuses); title writes `owner_id` — never a lease; a buying tenant's lease flips to `owner-occupied` with rent 0. `GS_DEEDS` + `GS_DEED_LOG` record title history; `gsDeedTick` runs the monthly obligation book (mortgage → county tax → HOA fund, all game dollars, partial pay to owed buckets, `missedN` counter, cure via `gsPayDeed`); `gsAdminForeclose` reverts title to the note holder after 2 misses (admin line on the wire). `license` request action + `gsLandlordEligible` (own a deeded door 30+ days, clean flags, rep ≥0) + capped scoped tools (`gsLandlordUnits/Statement/Notice/Raise/EvictFile` — the owner still evicts). Vacancy wheel: `gsListingTurnover` (vacate/evict → auto rent card), `gsListingFulfilled` (sign → card consumed), `gsListingWithdraw` (demolition pulls cards). `gsListingsBoard`/`gsListingCard`/`gsListingsPublic` (quiet-filtered) viewer surfaces; `gsListingSeedSF` posts the world board (9418-B $2100, 9457-2 $1350, 9127-A $1300) + quietly shops BOTH Victor Guerrero buildings (9418, 9457) — the canonical drama case. `gsListingAudit` proves addresses/conformity/privacy. Snapshot/load/reset ride the bus (`deeds` key). All bridged. |
| `systems/41_game_systems_reputation.js` | game v11 | THE STREET'S MEMORY — character-level reputation as a journal, not a bar. `GS_CREP` weights real lease/registry/feed events per side (`wL` landlord / `wT` tenant): for-cause evictions still bruise the tenant more than the landlord (-3 vs -14), wrongful (`noFault`) evictions hit the landlord hard and escalate on repeats (-16 → ×1.45 → ×1.9), dispute filings/losses are public record, quiet cures + private lateness earn standing back. Knowledge is bounded, never omniscient: parties + same-building learn on day 0, same street a day later, everyone else 2–6 days later (deterministic hash lag) — private financial events never reach the street at all; `gsCharRepTell` is the one-call gossip function. `gsCharRepScore` decays ~540-day half-life (nothing follows anyone forever); `gsCharRepStance` is a character's opinion from what THEY know (their own record ×1.5), bands warm/okay/watch/cold/pariah. `gsCharRepResponse` surfaces the measurable neighbors'-response (knowers/cold/organized at 3+ cold → 'neighbors comparing notes' wire beat). Behavior gates: `gsRepApplyCheck` (won't apply to a landlord whose record you know, once-burned never reapply), `gsRepTermCheck`/`gsRepMonthCheck` (cold tenants give notice at term-roll or on m2m). Public surface is band-and-count cards (`gsCharRepCard` — evictions/disputes/latenesses counts + band, never a float, public events only) + `gsCharRepBoard` for viewers (`gsViewerState().reputation`). `gsCharRepAudit` verifies score/knowledge integrity; seeded disputes journal at load; `gsCrepSnapshot`/`Load`/`Reset` ride the bus (`crep` key). Wired: leases apply-gate + term/m2m notice, feed lines (dispute filed/resolved, tenant notice, neighbors comparing notes — no names or details). All bridged on `__aiBridge`. |
| `systems/41_game_systems_onboarding.js` | game v12 | THE WELCOME WAGON — the viewer→player path as a real system (world/onboarding.json v53 contract + onboarding-ui.md copy deck). Journey records `GS_ONB.players` created only on the first knock (free observe needs nothing and creates nothing); stage derived, never stored (S0f fork → S1 tour → S1w watcher landing | S2 handle → S3 wallet → S4 first ask → S5 resident fork → S6 first day). Persona fork is emphasis-only — it never unlocks, hides, or reprices. Seven click-driven tour beats verbatim incl. the v53 Archive beat; skip-anywhere; never auto-plays. Handles (S2): format + reservation only (never §11-screened), reserved = every NV_CAST name token + hired names + system words, deterministic suggestions on taken; the Wire attributes by handle via `gsWireWho`. Wallet (S3): the six-pack ladder verbatim (pocket $0.99/100cr → mogul $99.99/14000cr), +50% first purchase once disclosed on-card, disclosed $200/PT-day spend cap, `gsWallet`/`gsWalletLedger` itemized receipts; ads stay the non-purchase mint. New bus action `camera` — compatible class, view-layer only (claims no in-world resource: never queues, never conflicts, auto-clears), the contract's 10 cr/30 min first ask. Guided lessons file REAL requests: 'decline' (costar on a thin pawn — the pawn's own vetoes decide, half back on a no), 'review' (weather parks for a human), 'queue' (weather into a claimed sky — −15% hold, `sky_free` is an honest refusal never a faked queue). `gsOnbHirePath` reads the real hire lane (job board + vacant stock + slots + 500cr-after-screening truth lines); `gsOnbHiredReturn` is the one-time first-day card on the possession-redacted briefing (secrets stay redacted for the hirer), rent owed in dollars, possession-as-visit, thin-AI-as-fact. `gsOnbLowBal` is the opt-in-only low-balance preview (reads the meter, moves nothing). Analytics ledger whitelisted to the contract's hooks, stage+opted_out envelope only. `gsOnbAudit` executes the never-list (ladder verbatim, handle pattern, cast names reserved, camera claims nothing at 10cr/30min, no completion-reward mints, ban intact, free feed stands alone). `gsOnbDeepLink` honors `?returning=1`/`?hired=1` — the only two links. Persistence rides the bus snapshot (`onb` key). All bridged on `__aiBridge`. |
| `systems/41_game_systems_economy.js` | game v13 | THE FRIDAY PAYROLL — the whole dollar circuit (roadmap v13, world/jobs.json held_by + budgets.json INTERNAL envelopes). `GS_ECON_WORK` runs canonical payroll for every working main + ambient: weekly Friday for entry/informal gigs, biweekly Friday (hash parity) for mid/top bands, monthly on the 1st for benefits/pension/owner draw; variable gigs jitter ±10% deterministically. Employer `biz:*`/`gov:*` accounts share the hiring layer's operating-float convention — top-ups are labeled faucet txns, payroll never bounces. `GS_ECON_FLOWS` moves the authored off-book cash (Jules→Carmen, Dani→Reyes cousins room shares) — ledger-faithful, feed-silent. THE NUT drains each resident weekly on their own shopping day (groceries → transit → utilities, partial-pays in order, unpaid remainder is a recorded shortfall never an overdraft); mains use authored household envelopes, everyone else the $490 baseline. Owner's back office: `gsEconBooks` (monthly statement: payroll by employer, benefits, informal, flows, nut, rent billed/collected/owed/latefees, float top-ups), `gsEconArrears` (collection queue: owed/owedBy/oldestDue/daysLate/openNotices/nextStep), `gsEconPayroll` (per-employer out/headcount/float incl. 41H hire wages), `gsEconStub` (per-resident money card, INTERNAL), `gsEconAudit` (full replay — derived==live to the cent, conservation, no econ credit txns, no negative banks). Feed policy: `payday` is the only public beat (one texture line, no names/sums); shares/shortfalls are withheld. Catch-up ticks every missed day bounded to 62; `gsEconSysTick` runs one econ day per real SF day on a 30s throttle; snapshot/load/reset ride the bus (`econ` key). |
| `systems/41_game_systems_timepiece.js` | game v13 | THE TIMEPIECE — pull-based time perception (rw-time-perception-spec, user-approved; fixes the Jules-in-the-rain clock confabulation). `sfAgentState` pushes NO clock — `senses` (generated light/weather/street-life line; venue open/dark per `GS_POI_HOURS` incl. parody↔real name twins) comes first in the payload, then `felt` (coarse anchor: "you last checked your phone 3 turns ago — it said 16:20" — elapsed is banded, never exact). Exact time is pulled: `sfAgentState(cid, {glance:'phone'|'wallclock'|'ask'})` — phone is exact; wallclock needs an enclosed `GS_CLOCK_PLACES` venue and returns that clock's OWN skewed time (Mudhaus/Haus +10, deliberate); `ask` quotes the nearest same-space pawn's own last check when fresh (<45 sim-min, skew propagates socially) else a fuzzy band guess. Every real check writes `v.lastClockCheck{said,at,day,source,turn}` — the pawn believes the LYING clock, which is the point. Rest/sleep is sheltered: `gsTimeShelter` routes home (schedule's inside-block → `v.sfHome` door) else nearest enclosed POI; outdoors-in-rain is refused with a suggestion. The standing directive (feedback item 2, ai-town §5) lives in 36_sf_agent.js: a brain files `order` + `directive` (`{verb,to,why,untilH,repeat}`, spellings `directive`/`act.directive`/`act.then`) — `v.sfDirective` is the durable will, promoted by `sfAgentNext` at order end through the same interpreter; sfNpcTick's ladder is reflex → order → directive → `intention_gap` (driven mains never fall back to code-authored `sfSched`); every order ends reported (`v.sfAgentResult`: completed/expired/failed/interrupted); stale-seq filings are rejected; why-less rest/idle, `request`, and unknown-verb directives are named-and-dropped at filing; chains carry; orders/wills use absolute sim-hours so nothing un-lapses past midnight. |
| `systems/41_game_systems_civic.js` | game v15 | THE MUNICIPAL CODE — conflicts, second pass (roadmap v15). VENUE ZONES: `GS_VENUE_ZONES` subdivides Dolores Park (north lawn/south lawn/playground/tennis courts/church street edge) + Precita Park (the lawn/the plaza); a permit claims `venue:<place>@<zone>` via `gsVenueZoneParse` ('place (zone)', bare trailing zone name, or `params.zone`) — same-zone and whole-place claims clash, distinct zones share, `bad_zone` refuses invented areas and zones on whole-venue kinds before billing; rest keys + surge pressure are zone-aware (`gsVenueResTouch`). NOISE ORDINANCE: amplified event kinds (block_party/street_fair/parade/movie_night) claim `noise:<place>` on top of their ground — the airspace clashes with every venue claim at the place including distinct zones — and rest 22:00–06:00 PT (`gsQuietOverlap`, O(1) unwrapped-PT math; a window ending exactly at 22:00 is clean); enforced identically at filing, quote, queue promotion, picker slots, and booking activation. CITY HOLDS: `gsAdminHold` declares a bounded closure on claim-space (`sky`, `venue:P[@Z]`, `noise:P`, `openair`, `char:*`, `paper:*`, `listing:*`) — sweeps overlapping live claims with full compensation, denies overlapping filings `city_hold` at the door, parks queued filings until lift (`gsLiveHolds`/`gsHoldCover`), slides bookings past where legal; `gsLiftHold` early + `gsCivicTick` expiry both emit a public `hold_lift` and promote the line; `gsHoldList` is the public board on `gsViewerState().holds`. CLERK'S ANSWERS: `gsReqOutlook` (queue position, blocking resources incl. hold ids, earliest likely start, booked window, review TTL) and `gsCivicAlts` (soonest legal slots for denied/held bookable asks) feed `gsPriceQuote`'s `alts`/`estStart`/`heldBy`/`slidesTo` fields. Holds ride the bus snapshot (`civic` key) + reset. Bridged: `gsVenueZoneList`, `gsHoldList`, `gsAdminHold`, `gsLiftHold`, `gsReqOutlook`. |
| `systems/41_game_systems_assessor.js` | game v17 | THE COUNTY RECORDER — the registry's municipal shadow (roadmap v17). `GS_PARC` puts every standing building on the assessment roll (kind `building`); a door that sells through the deed office carves its own condo parcel (own APN, own bill) while the building roll stops counting it. APNs mint `9xxx-0xx` (street-hashed block + sequential lot) — above every real SF assessor block, same privacy construction as the 9xxx house numbers; retired APNs are never reused. PROP-13 ROLL: assessed = min(market, Σ basis segments × 1.02^yr) − $7k homeowner's exemption; market derives live from the rent roll via the deed office's grm convention so subdivision/demolition moves it for free; `gsAssessSale` rebases on change of ownership, `gsAssessImprove` adds a new-construction segment (garage conversions), `gsParcelRetire` freezes the record on demolition. SECURED ROLL: real CA calendar — inst 1 posts Nov 1 / delinquent Dec 10, inst 2 posts Feb 1 / delinquent Apr 10, 10% late penalty, balances open at fiscal-year end (Jun 30) default publicly; bills auto-collect from the owner's dollar account, `gsPayTaxBill` is the redemption window, redemption clears the parcel flag only when nothing else is defaulted. Deed-book doors pay monthly impound instead (`taxMo` re-syncs from the roll each tick — no double-billing). RENT BOARD: $59/controlled-unit fee rides installment 1 in full; a parcel unpaid through the current FY is unregistered and `gsRaiseRent` refuses (`rentboard_unregistered`) — the real registered-to-raise rule. PUBLIC RECORD: `gsParcelView`/`gsParcelByApn`/`gsTaxRoll`/`gsCountyStats` (bridge + `gsViewerState().county`), delinquency/default/redemption beats print on the wire via `county` events → `housing` lines (address only, never amounts), `gsAssessorAudit` runs beside `gsRegistryAudit`. One `gsAssessorTick`/day (walk-bounded), snapshot via `assessor` key, all game dollars. |
| `systems/41_game_systems_errands.js` | game v18 | THE ERRAND LAYER — possession's in-world verbs (roadmap v18). Boards: `GS_BOARDS` mirrors world/storefronts.json v44 verbatim — parody names, game-dollar prices, trailing-price parse (unsellable chalk lines stay display-only), open-air stalls (Growers Market, Las Palmas) sell at the door. Verbs: `gsPossessEnter/Exit` (door proximity ≤5 cells, 'home' reaches the hire's own lease door), `gsPossessMenu` (the board where the pawn stands), `gsPossessBuy` (item -> `biz:*` till accounts shared with payroll; food/drink bump real body gauges + the thin-needs model so the AI doesn't re-errand; goods persist as `h.belongings`; session spend cap counts), `gsPossessGreet` (the only social verb — NO text parameter exists, the hello is the shared thin kit's line; mains get a real `sfConvoOpen`+`convo_invite` dispatch and decide for themselves, thin/hired/co-driven pawns answer from the same kit; 20-min pair cooldown), `gsPossessRest` (real sit/rest state — bodyTick recovers), `gsPossessScene` (the driver windshield: pos/inside/venue+menu/nearby chars/body gauges/meter+extend/acts/rentRisk). All verbs gate session+driver+body; enter/exit/rest feed beats stay raw-logged but wire-suppressed; buy/greet/bodywarn print attributed lines. Snapshot `errands` key (greet cooldowns); `gsErrandReset` rides the bus reset. Bridge: `gsPossessEnter/Exit/Buy/Greet/Rest/Scene/Menu`, `gsErrandBoard`. |
| `sf/38_sf_brain.js` | game v16 | THE BECOMING BRAIN — dispatch + interiority for the 8 driven mains (brain-unified-design, dispatch regime). `sfBrainTick` (registered via `registerSimTick`, never wrapped) scans each main per sim tick: `sfDispatchTick` queues deduped triggers (order_end on failed/interrupted, gap, directive_expiry <=0.25h lookahead, convo_invite/floor/end, salient arrivals + witnessed collapse, intent_fired, needs-band entry T0/T1, env transitions, heartbeat ~3.5 waking sim-h; sleepers get exactly ONE mid-sleep T0 refile + cueType:'time' intent alarms, events queue for wake). `sfDispatchPoll` is the driver's drain surface: rate caps (0.25 sim-h spacing except ~0.04 sim-h convo floor cadence, 10 calls/sim-h hard stop), `due{kind,tier,detail}` + `blocked` reasons + stale-dispatch re-surfacing. `sfDispatchServed` records the call on every accepted `sfAgentAct` filing. CONVERSATION CHANNEL: `sfConvoOpen/Say/Pass/Leave/End/Tick/State` — invited→participating→ended|dropped, shared `SF_CONVOS` logistics record, floor bookkeeping (`yourTurn`; say flips it, any served act passes it, `do` beats ride the tail), '?'-line obligations that outlive the convo as `sfObligations` urges, caps (16 utt / 1.5 sim-h), 0.5h invite TTL, ~10-cell proximity gate with 0.15h grace, 'no_answer'/'convo_ended' interrupt live talk orders, per-party POV `observe` writes + thinned overhear. ARMED INTENTIONS: `sfIntentArm`/`sfIntentScan` — `act.intent{deed,condition,cueType,when,who}` parks on `v.sfIntents[]`; event cues fire on perceived `who`/venue-name match, time cues are alarms; firing surfaces the intent AND dispatches 'intent_fired' (ratification-in-context, never auto-execution). MIND: `sfMindScan` builds the `mind` block — mood echo, concerns, surface (fired intents -> obligations -> stale-intent urges -> cue-matched recalls, intrinsic weight, <=4), nearby-person `people` models (stance/open/vibe from bonds + obligations), `lastTurn` why-echo, armed intents list. `sfReflectArchive` is the ?reflect=1 raw material (top-salience records since last reflect + open loops + intents). Bridged: `sfDispatchPoll`, `sfMindScan`, `sfConvoState`, `sfReflectArchive`. |

### `render/` — drawing
| module | from | contents |
|---|---|---|
| `render/05_render.js` | PART 6 | render pipeline, camera, sprite drawing |
| `render/12d_overlay.js` | PART 12D | overlay: piles, campfires, crops, chickens, wildfire |

Pixel-art source modules (`pa-core/terrain/veg/props/buildings/chars/fx`) are
**not** duplicated here — they live in `willowbrook/dev/pa-*.js` and are
bundled first (PART 1). They are shared with the original Willowbrook game, so
they stay in place rather than being copied.

### `ui/` — interface
| module | from | contents |
|---|---|---|
| `ui/08_hud.js` | PART 9 | HUD, RimWorld-style Pawn Inspector |
| `ui/10_controls.js` | PART 11a | buttons, speed, pause, pawn selection controls |

The HTML shell and all CSS live in the bundler (`scripts/build_willowbrook_natura.py`,
`HTML_TEMPLATE`) — not in `src/`.

### `data/` — static data
| module | from | contents |
|---|---|---|
| `data/03_roster.js` | PART 5 | founding villager definitions, `initVillagers`; Phase 2E: canonical `createVillager` entity factory |
| `data/recipes.js` | Phase 2A | `RECIPE_TABLE` (wood chain: fell_tree/plank/furniture; grain chain: harvest_crop/flour/bread), `getRecipe`/`findRecipe`, provenance API (`createProvenance`, `calcSkillQuality`, `recordUsage`, `attachItemProvenance`, `getItemProvenance`). Transfer API: `stripItemProvenance` (called on every inventory decrement — drop/eat/burn/sell/craft/mend/build/cook/preserve/feed/recipe-inputs — so no ghost records survive), `addItemProvenance`, pile-side `p.prov` store via `pileProvList`/`movePileProvenance` (records ride with dropped/taken items; take prefers real pile records, falls back to the legacy `felledBy` tag). NOTE: `tools` are descriptive metadata — no tool items exist in inventories yet, so all `toolsRequired` are `[]`; the executor gates on them automatically once tool items exist. |

### `brain/` — decision-making & external interface
| module | from | contents |
|---|---|---|
| `brain/07_ai.js` | PART 8 | rule-based villager AI, `updateVillagerAI`, player pawn controls |
| `brain/09_bridge.js` | PART 10 | `window.__aiBridge` (listVillagers/getPerception/postAction/…); Phase 2E: getLifeStage, getHousehold, getRelationships, getOccupation, marry helpers |
| `brain/12b_perception.js` | PART 12B | honest perception (replaces leaky v1) |
| `brain/13a_intent.js` | PART 13A | plain-word intent planner, dreams, capability gaps |
| `brain/14_substrate.js` | Phase 6E | FeelingSubstrate interface (S1: feel, getLayers, getFeelingScape expanded to 12 qualities: hollow, parched, heavy, burning, anxious, terrified, enraged, content, lonely, revered, confused, vigilant), canonical world-event->signal table (A3: WORLD_EVENT_SIGNAL_TABLE, normalizeEventToSignal), 3 decay accumulators (D1: hungerStressAcc, fearFatigueAcc, painPatienceAcc with analytical dt-scaled integration), D2 vocabulary reading conditions[] (burnt-throat test), and S2 substrate lint helper with bracket access check. |
| `brain/knowledge.js` | Phase 2D | Individual epistemic store: memories, active beliefs, ownership beliefs, claims & evidence. Separation of reality vs perception vs memory vs knowledge vs belief vs claim vs evidence vs uncertainty. Core API: `observe`, `getBelief`, `knowsAbout`, `getOwnershipBelief`, `recordOwnershipBelief`, `claimOwnership`, `teach`, `decayEpistemic`. Teaching mechanic with proximity & consciousness gating. Sim-time forgetting with personality fidelity presets (sharp / average / forgetful) and salience weighting. Superseding of conflicting beliefs with history preservation. Generalized conflict detection: ANY shared content key with a different value supersedes (not just 5 hardcoded keys). History integrity: beliefs deep-clone evidence/content (beliefFromMemory) so reinforcement never mutates historical entries. Opt-in observation dedupe (details.dedupeWindowH): equivalent recent observations reinforce without pushing duplicate memories. Utility AI integration: consults beliefs before omniscience for food targets, exploring when unknown. Verbs: `teach`, `claim`. `__aiBridge` inspection helpers. Phase 2E: teaching learning bonus for child/youth students (+15% confidence/salience). |
| `brain/utility.js` | Phase 2C | Utility AI: candidate action evaluation (eat, drink, sleep, work, cook, craft/recipe, clean, socialize, flee, douse, trade, warm, rest, leisure), scoring across 8 need deficits, personality weights (industrious/lazy/sociable/cautious/brave/gluttonous), distance/proximity cost, risk modifiers (beasts, fire, night), opportunity bonuses (caravan in town, inn meal, fresh bread). Deterministic seeded tie-breaking (no Math.random). Honest failure handling: failure -> observation thought -> interpretation -> new knowledge (v.unreachable) -> re-evaluation -> new action. Bridge contract (__aiBridge) intact. Epistemic integration (2D): candidate generation consults character beliefs, falling back to explore when food locations are unknown; pile re-observation deduped (dedupeWindowH 6h) to avoid memory spam. |

### `tests/` — in-page autotest suite (`?test`)
| module | from | contents |
|---|---|---|
| `tests/12d_autotest.js` | PART 12D | parity systems tests |
| `tests/13_autotest.js` | PART 13 | intent/economy/butcher tests + test factory |
| `tests/14_autotest.js` | PART 14 | medical/rescue/wildlife/skills/build/clothing tests |
| `tests/15_autotest.js` | PART 15 | food/husbandry/caravan/friction/firefight tests |
| `tests/16_autotest.js` | Phase 2A | recipe table lookup, plank/furniture/flour/bread production, provenance fields, input gating, intent compilation, felled-log pile provenance |
| `tests/17_autotest.js` | Phase 2B | building entity properties, indoor temp dynamics (fire warms, winter cools), capacity & overcrowding penalties, ownership tracking, expansion, abandonment decay, demolition material reclamation, river worldgen & bridge crossing & drinking/fishing, functional buildings integration (barn, workshop, kitchen, inn), intent parser compilation |
| `tests/18_autotest.js` | Phase 2C | starving scores eat above work, exhausted chooses sleep, beast nearby makes flee outscore foraging, personality (industrious vs lazy) changes work/leisure ranking for identical needs, unreachable food target -> thought recorded + re-evaluation picks alternative, determinism with seeded RNG, __aiBridge contract intact + utility inspection, survivalGuard hard safety net, caravan/inn opportunity bonuses, distance proximity cost, cleanliness deficit cleaning, social deficit chat elevation |
| `tests/19_autotest.js` | Phase 2D | 15 automated assertions: direct observation yields high confidence belief, student taught by teacher gains hearsay knowledge with reduced confidence, forgetting over sim days with sharp vs forgetful presets, high salience persistence, false beliefs contradicting world truth, superseded beliefs with history retention, ownership belief structure with evidence/claims, claim recording without mutating actualOwner, third-party claim witnessing, proximity-gated teaching, consciousness-gated teaching, unknown topic null query, utility AI belief-driven food targeting vs exploration, 19.14 regression (non-hardcoded key conflict supersedes instead of silently merging), 19.15 regression (belief reinforcement does not mutate historical evidence array). |
| `tests/20_autotest.js` | Phase 2E | 26 automated assertions: deterministic life stage transitions (child->youth->adult->elder), child heavy verb blocking, elder slower movement & heavy labor blocking, marriage gating (mutual bond, adult, proximity, consciousness), wedding memories recorded in both spouses & witnesses, death honestly clears spouse link without dangling spouseId, household unit shares home building & members, birth creates child with parentage + household membership + 2D memories, childcare consumes REAL food stock (conservation asserted) + honest failure thought when stores empty, bathe raises hygiene to 1.0, bonds decay without contact, occupation emerges from sustained activity, occupation bonus PROVEN as exactly 1.25x farm progress through the real executor chain, play preferred by children in utility scoring, market stall trade with inventory exchange & gold conservation, trader occupation earns real price discount (5g→4g), unmarried pair never conceives over 40 days, birth deterministic (same mother+day → same name/sex), failed marriage plan leaves explanatory thought, person-targeted 'go' resolves to coords + NaN targets never corrupt position, blocked-destination movement arrives without treadmill, critical thirst beats nearby wolf (drink plan issued), wall-following navigates around building to the well (no trap), critical-thirst villager at well with drink plan survives to drink (mortality-order), exhausted villager with sleep plan survives to sleep, dehydrated player pawn gets survival drink plan (no idle death). |
| `tests/21_autotest.js` | Phase 2F | 28 automated assertions: stable identified object id/creator/owner/holder/create-event; chair->plank->tree lineage through real crafting; gift changes owner+holder with item+world log entries; unseen theft moves holder only (owner stays, thief knows truth); victim missing-item notice + true belief; caught theft leaves item in place with victim/witness beliefs, memory, bond damage; borrow records expectedReturnH and giveback restores; refused borrow is honest failure; overdue-borrow query + lender/borrower notices; lose/find keep owner; abandon keeps owner; claim never alters owner; conflicting claims open dispute; deterministic evidence-weighted resolution with permanent resolution event + loser grudge; bulk goods stay count-based; taking another's held item honestly refused; stranger belief lookup leaks nothing while witnesses of public gifts form honest beliefs; failed claim on nonexistent item fails honestly; a late third claimant joins an open dispute and is scored in resolution; a claim conflicting with a settled dispute reopens a fresh dispute (history preserved); the AI bridge never exposes actualOwner in any getter while viewers get belief-scoped data; no borrow/steal candidates when inputs are already held (hasCraftInputs guard); a claimant mayor cannot decide their own dispute; the bridge transfer log redacts true-ownership tails ("ownership stays X", "(owner still X)", "(still owned by X)") from steal/find/abandon events while the internal history keeps the full record. |
| `sim/22a_save.js` | Phase 3 | Versioned save/load of the ACTUAL sim state (localStorage in browser, in-memory fallback): world clock/weather/pressure systems, mutated chunks (typed arrays packed as {__ta,d}), full villager records (body/needs/skills/bonds/inventory/plans/pregnancy/wounds/memories/epistemic/thoughts/why-trace), identified-item registry + ITEM/TRANSFER/TREE/epistemic id counters, transfer log, piles/fires/crops/chickens/wildlife/livestock/carcasses/fences/coops, felled trees, caravan/shop/inn, households, event log, UI indices, and RNGS.s (seeded RNG stream) so continuation is bit-identical. `currentAction` (transient, may hold live otherPerson refs) excluded; `_ci` preserved as-saved. Manual slot + autosave-once-per-day via simTick wrap; version mismatch / bad JSON rejected without mutating the world. Bridge: saveGame/loadGame/worldHash/hasSave. |
| `brain/22b_why.js` | Phase 3/6E | Why-action inspector: wraps (never replaces) `evaluateVillagerUtility`, recording only the LAST decision per villager — winner, top-6 candidates with scores, need deficits + personality weights + night flag that fed the scores, per-candidate distance, and feelingScape. `__aiBridge.explainAction(name)` returns structured trace with feelingScape. Pawn inspector gains a "🧠 Why this action?" section refreshed inside updateHUD with float feelings purged in favor of feeling qualities in words. |
| `render/22c_overlays.js` | Phase 3 | Five independently toggleable debug overlays, ALL OFF by default (draw function returns immediately when all off): needs bars, ownership labels (holder + inspected villager's believed owner — belief-scoped), belief-confidence view (known/suspected/unknown) for the inspected villager, building/farm zones, event feed. Wraps drawParityOverlay; no competing renderer. Bridge: debugOverlays()/setDebugOverlay(). Browser: 🛠 Debug button + checkbox panel. |
| `tests/22_autotest.js` | Phase 3 | 14 automated assertions: saveGame produces a non-empty blob; worldHash stable without ticks; RNG stream state (RNGS.s) restored exactly; save→12 ticks→hashA vs load→12 ticks→hashB bit-identical; save→load→save byte-identical; incompatible version rejected cleanly with world untouched; identified-item id counters survive (no id reuse); open dispute with both claimants survives; utility decision records trace (winner + candidates); trace carries need/personality/distance score components; explainAction structured, belief-scoped, no actualOwner leak; overlays draw nothing when all off; needs overlay toggles on/off cleanly; autosave fires via simTick hook. |
| `tests/23_autotest.js` | Phase 4 | 17 automated assertions: year/season cycling across 5 years without permanent winter, live midnight year-rollover, winter crop growth pause & spring resumption, forage registry equivalence, bush registry regrowth, save dirty-chunk tracking, transfer log archiving, W.year persistence, caravan seasonal trade arrival, well orbit escape, and lethal triage precedence. |
| `tests/24_autotest.js` | Phase 6A | 12 automated assertions: 3-gate selective attention system (Gate 1 hostile-only beast and uncontrolled wildfire preemptive interrupt, docile wolf causes 0 plan wipes across 40 ticks matching control travel, douse plan preserved; Gate 2 biological emergency >75% tunnel vision blocking non-food memory; Gate 3 top-down goal relevance), expectation engine (bread price shock -> surprise event + disappointment emotion + replan flag; stale stash location belief superseded with preserved history; matching expectation reinforcement with zero overhead; expectation expiry after 7 sim days; real gameplay take step at emptied stash triggers surprise on arrival without bypassAttention cheat; out-of-stock failed purchase does not anchor expectation), deterministic attention scanning with seeded FNV-1a hashString18, typed PerceptionDTO boundary observe -> think -> learn pipeline, and clean test cleanup. |
| `tests/25_autotest.js` | Phase 6B | 10 automated assertions: dynamic candidate generation on blocked action (<= 3 candidates from goal templates + beliefs + habit with transparent reasons and deterministic tie-break sort), WHY inspector integration (explainAction, decision.winner.reason, candidate reasons, #pi-why HUD), carrying capacity & bush depletion (1.0 -> 0.4 -> <= 0; winter regen = 0; spring regrowth), 4 seasons ecology (winter crop pause vs summer 1.2x growth; winter bear hibernation; winter tree pause), 2-state wolf AI (sated wolf flees torch; extreme hunger >= 0.8 overrides fear), weather-fire causal chain (dryDays -> dryness factor, rain/snow accelerated extinguishment, terrain/structure fuel evaluation), tile burned to ash (chunk scorched, vegetation cleared, ash pile with provenance 'burned'), firefighting system priority and safe douse, performance sanity probe (soulTick on populated village of 20 villagers across 3 game days / 144 ticks), and clean entity cleanup without leaks. |
| `tests/26_autotest.js` | Phase 6C | 9 automated assertions: Tier 1 conflicting motivations (Alden 70% fatigue + sick person -> duty beats sleep, suppression stress + tired-but-proud emotion recorded into memory), Tier 2 biology override (Alden 95% fatigue -> forced microsleep despite duty), Survival Guard with death-threshold triggers & anti-oscillation lock stable >= N ticks (anti eat<->flee loop), Autobiographical Identity crystallization from extreme-salience events (>= 0.90) with 5-tag cap and Pip's mother 5x cry attention multiplier, Body-Factor Chain (untreated wound -> infection -> workFactor drops -> autonomous preference for lighter work), Emotion Derivation Layer (hungry->anxious, pain->suffering, stable->content + decay), blood guard trigger boost (blood=0.5 -> rest/tend candidate boosted >=8000 and picked over work), real gameplay crystallization of all 7/7 identity tags (pips_mother, betrayed, fire_savior, near_drowning_survivor, master_craftsman, master_farmer, village_protector via real gameplay paths with zero manual formIdentityTag), and clean entity cleanup without leaks. |
| `tests/27_autotest.js` | Phase 6D | 3 automated acceptance assertions: T1 (salt closed loop, multi-trip: salt stock 0 -> price high -> doBuyStep out-of-stock records demand -> caravan brings demand-driven cargo -> stock restored -> price falls -> second arrival with zero demand brings base only), T2 (thief caught red-handed: real doStealStep with conscious witness -> witness trust <= 0.05, thief ostracized -> guard vacancy elects honest candidate, thief disqualified with score -1), T3 (false rumor: spreadGossip without crime -> listener holds theft-rumor reason with non-direct source -> trust <= 0.05). |
| `tests/28_autotest.js` | Phase 6E | 18 automated assertions: 28.1 (S2 substrate lint rule & deliberate bypass test catching sneaky v.body. / v["body"].), 28.2 (S1 FeelingSubstrate feel, getLayers, getFeelingScape contract), 28.3 (A3 canonical world-event->signal table & ingestion), 28.4 (D1 Coupling 1 hunger->stress accumulation & decay to ~0), 28.5 (D1 Coupling 2 fear->exhaustion 3h window boost & expiration), 28.6 (D1 Coupling 3 pain->patience reduction & social utility penalty), 28.7 (determinism across tick sizes with analytical dt-scaling), 28.8 (A1/D2 12 Core Qualities scape merge), 28.9 (D2 burnt-throat vs hydration differential test & fire overrides content), 28.10 (WHY HUD float purge & text display), 28.11 (Fix B1 Nửa 1: live A3 arrows for 5 signals death/downed/gossip/birth/marriage routing to active qualities), 28.12 (Fix B1 Nửa 2: real production-path callers for wildfire, death, downed without manual injection), 28.13 (A2 Physical acoustic sound propagation with distance falloff & negative control), 28.14 (A2 sleeping guard acoustic interruption waking sleeping guard), 28.15 (3B stressResidue lifecycle: 0.35*maxStress hard ceiling clamp, assertCeiling throw on breach, background unease, 3-day recovery to near-zero), 28.16 (NN1 co-channel fire deduplication peak-hold & 3B workFactor impact), 28.17 (Gareth bravery parity with hunting=3 vs non-Gareth control), 28.18 (entity cleanup preserving canonical settlement). |
| `tests/29_autotest.js` | Phase 7A | 5 automated assertions: 29.1 (Customary court presiding elder selection by age and reputation + local belief witness gate with far witness rejected - negative control 3d), 29.2 (Mistaken witness belief -> wrongful court conviction -> grievance reason with decay -> emergent retaliation narrative - 3a), 29.3 (Restitution transfers real currency between parties preserving material conservation + decaying court-verdict reason), 29.4 (Guilds membership and 30-day apprentice skill gain > self-taught, outsider receives zero bonus - negative control 3b), 29.5 (Guild caravan bloc sales of 10 bread achieves unit price > retail single unit price, outsider receives retail price only - negative control 3c, material conserved). |
| `tests/30_sf_autotest.js` | SF scenario | SF-mode-only suite: 28 cast spawned, OSM grid decoded, POIs/doors registered, building interiors block + door thresholds walkable, POI-to-POI sidewalk pathing, door-teleport interiors, all animation states render in-world. |
| `tests/41_game_systems_autotest.js` | game v0 | Mode-agnostic game-systems skeleton suite: SF-mode block asserts the live seeded registry (9xxx formula conformance, per-street uniqueness, cast leases — Carmen on Guerrero, Jules off-lease, Victor owner-occupied, Priya+Marcus occupants). Unit block (reset state, restored after): spec formula + range + determinism, address format, lease sign/turnover/evict, clerk minting + mint log, retired numbers never reused, registry/ledger/bus snapshot round-trips, credits grant/spend/refund/overdraw, dollar conservation, rent collection + honest insufficiency, possession ban for all C1-C8 (incl. owner, before billing), hired-only possession, duration/unknown/target denials, pairwise conflict matrix (contradictory-weather clashes, identical-forecast co-sponsorship, severe-sky × outdoor permits, one-permit-per-venue normalization, paperwork serialization), compatible multiplayer sessions, strict global FCFS promotion incl. 3-deep queues + no leapfrogging, cancel→instant promotion, queued-expiry full refund, per-player + global cooldowns, cancel/admin-revoke refunds, feed monotonic + all lifecycle event kinds. **v5 possession block**: character-kind vocabulary (`possession_ban`/`cast_ai_only`/`not_your_character`), canonical-code intent screening before billing (harm-targeting/legal-backstop/secret-extraction/possession-scope/admin-domain — owner bound too), briefing audit proves no inner-life key leaks, live session suspend/resume + metered viewer view, one-shot wind-down warning, timeout/release/admin-revoke end reasons in `GS_POSSESS_LOG`, orphan sweep, driver verbs (drive waypoints, dollar spend, landlord rent settlement, rent-risk flag), FCFS promotion re-binding sessions, driving record in snapshots, deterministic hired looks + full-day routines, real SF pawn spawn (frameset/design/VILLAGERS), medieval no-spawn, cast-cap denial + `gsReleaseHired` freeing seats, and the §11.4 review lane (park billed, approve activates fresh, deny refunds, lapse auto-refunds, cancel/revoke reach in, repeat-pattern + appeal escalation). **v12 onboarding block**: fresh-id free-observe (no record, no credits), persona fork + tour beats + checklist truthfulness, handle format/reserved/taken/suggest + wire attribution, pack ladder + first-purchase bonus + spend cap, ad path, camera action (10cr/30min, claims nothing), guided lessons filing real requests (decline→costar half-refund, review→weather parked, queue→−15% hold behind a real sky), first-ask→hire walkthrough entirely through `gsSubmitRequest`, S6 first-day card privacy, low-balance opt-in preview, exit states, deep links, snapshot round-trip, never-list audit. **v15 municipal block**: zone table + grammar (parens/bare-suffix/params.zone, bad_zone denials), same-zone rest denial vs cross-zone sharing, amplified airspace claims + the quiet-hours boundary at 22:00 sharp (filing, picker, book), co-host merge + last-sponsor teardown + the public 'cohost' wire line, city-hold sweep/compensation/door-denial/queue-parking/lift promotion incl. the `hold`/`hold_lift` admin feed vocabulary, `gsReqOutlook`/`gsCivicAlts`/`gsPriceQuote` receipts, hold snapshot round-trip. |

## Dependency notes for Phase 2

- Later PARTs **wrap** earlier functions (`const __baseBodyTick = bodyTick; …`
  in 12A; `boot = function(){…}` override in 14F). Keep 14F/15F wiring modules
  after the modules they patch.
- `sim/11_boot.js` references almost everything; it must stay after PARTs 2–10.
- Test modules are self-contained but rely on the full sim; they run only under `?test`.
- Phase 2 may further split large modules (e.g. `systems/12c_actions.js`,
  `render/05_render.js`) — the `_order.txt` list is the only ordering contract.
